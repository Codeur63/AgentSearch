[project]
name = "agentsearch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.16.0",
    "groq>=1.7.0",
    "langchain>=1.4.1",
    "openai>=3.14.1",
    "pydantic-ai>=2.44.0",
    "python-dotenv>=1.2.3",
    "temporalio>=1.33.0",
]

[project.scripts]
agentsearch = "agentsearch:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
